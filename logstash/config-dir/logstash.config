input {
    gelf {
        port => 12201
    }
}

output {
    elasticsearch {
        hosts => ['elasticsearch_a','elasticsearch_b','elasticsearch_c']
        index => "logstash-%{+YYYY.MM}"
    }
}
